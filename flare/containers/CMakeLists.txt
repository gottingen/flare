#need these here for now
FLARE_INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})
FLARE_INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

#-----------------------------------------------------------------------------

SET(FLARE_CONTAINERS_SRCS)
APPEND_GLOB(FLARE_CONTAINERS_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/*.cc)
SET(FLARE_CONTAINER_HEADERS)
APPEND_GLOB(FLARE_CONTAINERS_HEADERS ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

FLARE_ADD_LIBRARY(
  flare_containers
  SOURCES ${FLARE_CONTAINERS_SRCS}
  HEADERS ${FLARE_CONTAINERS_HEADERS}
)

FLARE_LIB_INCLUDE_DIRECTORIES(flare_containers
  ${FLARE_TOP_BUILD_DIR}
  ${CMAKE_CURRENT_BINARY_DIR}
  ${CMAKE_CURRENT_SOURCE_DIR}
)
FLARE_LINK_INTERNAL_LIBRARY(flare_containers flare_core)

#-----------------------------------------------------------------------------
